# Keep only source and web assets in repo
# Ignore everything except lib/, web/, README.md, pubspec.yaml, pubspec.lock, and .gitignore itself

# Basic ignores
.DS_Store
*.log
*.tmp

# Flutter / Dart
.dart_tool/
.packages
.pub/
build/

# IDEs
.idea/
*.iml
.vscode/

# Platform folders (we will keep lib/ and web/ only)
android/
ios/
linux/
macos/
windows/

# Generated files
*.g.dart

# Secrets and keys
**/google-services.json
**/GoogleService-Info.plist
*.jks
*.keystore
secrets.yaml

# Other
pubspec.lock

# Allow the files we want
!lib/
!web/
!README.md
!.gitignore
!pubspec.yaml
!pubspec.lock
